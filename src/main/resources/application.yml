server:
  port: 8000

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9000/eureka

spring:
  application:
    name: api-gateway
# ? ??????? ?? ?? ??
#  cloud:
#    gateway:
#      routes:
#        - id: spring-service
#          uri: lb://SPRING-SERVICE
#          predicates:
#            - Path=/spring-service/**